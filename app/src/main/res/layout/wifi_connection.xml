<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white"
    android:transitionGroup="false"
    android:textAlignment="center">

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btnPair"
        android:id="@+id/btnPair"
        android:layout_marginTop="137dp"
        android:minHeight="50dp"
        android:minWidth="150dp"
        android:background="@android:color/darker_gray"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:nestedScrollingEnabled="false"
        android:onClick="buttonOnClick"
        android:visibility="invisible" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btnShowDevice"
        android:id="@+id/btnShowDevice"
        android:minHeight="50dp"
        android:minWidth="150dp"
        android:background="@android:color/darker_gray"
        android:layout_below="@+id/btnPair"
        android:layout_alignLeft="@+id/btnPair"
        android:layout_alignStart="@+id/btnPair"
        android:layout_marginTop="69dp"
        android:visibility="invisible" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Available Networks"
        android:id="@+id/txt_AvailableNet"
        android:textColor="@android:color/darker_gray"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="38dp" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/lst_ShowNet"
        android:layout_below="@+id/txt_AvailableNet"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:textAlignment="center"
        android:choiceMode="multipleChoice" />

</RelativeLayout>
